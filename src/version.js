// Kept as a plain constant rather than read from package.json: the CLI is
// distributed as a compiled single file by Homebrew, where package.json is not
// alongside it at runtime.
export const VERSION = "0.1.0"
